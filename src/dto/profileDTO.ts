export type updateInfoDto = {
  error: string;
  success: boolean;
};

export type editableFieldsDto = {
  name: string;
  phone: string;
  faculty: string;
};

export type ineditableFieldsDto = {
  email: string;
  date_expire: string;
  studentId: string;
  status: string;
};
